local M = {}

function M.config()
    -- vim.keymap.set(
    --     "n",
    --     "<Leader>i",
    --     "<Cmd>lua require('apyrori').find_and_insert_match(vim.fn.expand('<cword>'), nil, false)<CR>"
    -- )
end

return M
